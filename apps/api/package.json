{
  "name": "@citenook/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run fastapi dev app/main.py --host 0.0.0.0 --port 8000",
    "worker": "uv run python -m app.worker",
    "build": "uv run python -m compileall -q app",
    "lint": "uv run ruff check app tests",
    "test": "uv run --extra llamaindex pytest -q"
  },
  "license": "Apache-2.0",
  "author": {
    "name": "Keresztes Zsolt",
    "url": "https://kereszteszsolt.hu"
  }
}
